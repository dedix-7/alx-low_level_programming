#include "main.h"
/**
 * _strcpy - copy a string
 * @dest: destination string
 * @src: source address
 * Return: address to dest
 */
char *_strcpy(char *dest, char *src)
{
  while ((*(dest++)) = ((*(src++))));
	return (dest);
}
